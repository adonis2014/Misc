
f{char} 命令让Vim查找下一处指定字符出现的位置,如果找到了,就直接把光标移到那里
;       命令会重复查找上次 f 命令所查找的字符
.       命令会“重复上次修改”
a       命令在当前光标之后添加内容
A       命令则在当前行的结尾添加内容
,       键跳回去,这个命令会反方向查找上次 f{char}所查找的字符
cw      删除光标位置到单词结尾的字符，并进入插入模式

在插入模式中, <C-r>=3*4<CR> 会计算3*4的结果并填在对应位置
